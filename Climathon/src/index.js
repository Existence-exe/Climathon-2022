AOS.init();


ityped.init('#topic', {
    strings:['Climathon 2022', 'Project by Team', 'Alohomora'],
    startDelay: 500,
    loop: true,
    typeSpeed:  80,
    backSpeed : 60,
    cursorChar: "|"
});